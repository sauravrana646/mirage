---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: previewtemplates.mirage.dev
spec:
  group: mirage.dev
  names:
    kind: PreviewTemplate
    listKind: PreviewTemplateList
    plural: previewtemplates
    shortNames:
    - pt
    - previewtemplate
    singular: previewtemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.resources.preset
      name: Preset
      type: string
    - jsonPath: .spec.networkPolicy
      name: Network
      type: string
    - jsonPath: .spec.ttlSeconds
      name: TTL
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PreviewTemplate holds platform defaults for PreviewEnvironments.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PreviewTemplateSpec defines platform defaults for PreviewEnvironments.
            properties:
              backend:
                default: direct
                description: 'Backend default: direct or argocd.'
                enum:
                - direct
                - argocd
                type: string
              containerPort:
                default: 8080
                description: ContainerPort default for single-service previews.
                format: int32
                type: integer
              ingress:
                description: Ingress defaults (host derived from domain when enabled).
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations merged onto preview ingresses.
                    type: object
                  domain:
                    description: Domain is appended as `<preview-name>.<domain>` when
                      host is unset.
                    type: string
                  enabled:
                    description: Enabled defaults ingress on for previews that do
                      not override it.
                    type: boolean
                  ingressClassName:
                    description: IngressClassName default.
                    type: string
                  path:
                    description: Path default for ingress.
                    type: string
                  tls:
                    description: TLS defaults.
                    properties:
                      enabled:
                        type: boolean
                      secretName:
                        description: SecretName for the TLS certificate (cert-manager
                          can create it).
                        type: string
                    type: object
                type: object
              livenessProbe:
                description: LivenessProbe default.
                properties:
                  initialDelaySeconds:
                    default: 10
                    format: int32
                    type: integer
                  path:
                    default: /
                    type: string
                  periodSeconds:
                    default: 10
                    format: int32
                    type: integer
                  port:
                    default: 8080
                    format: int32
                    type: integer
                type: object
              networkPolicy:
                default: baseline
                description: NetworkPolicy default mode.
                enum:
                - baseline
                - permissive
                - disabled
                type: string
              readinessProbe:
                description: ReadinessProbe default.
                properties:
                  initialDelaySeconds:
                    default: 10
                    format: int32
                    type: integer
                  path:
                    default: /
                    type: string
                  periodSeconds:
                    default: 10
                    format: int32
                    type: integer
                  port:
                    default: 8080
                    format: int32
                    type: integer
                type: object
              replicas:
                default: 1
                description: Replicas default.
                format: int32
                maximum: 10
                minimum: 0
                type: integer
              requireDigest:
                description: RequireDigest default.
                type: boolean
              resources:
                description: Resources size class for workloads that omit explicit
                  resources.
                properties:
                  custom:
                    description: Custom overrides the preset when set.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  preset:
                    description: 'Preset is a named size: small, medium, or large.'
                    enum:
                    - small
                    - medium
                    - large
                    type: string
                type: object
              security:
                description: Security profile for preview namespaces.
                properties:
                  profile:
                    default: restricted
                    description: Profile is restricted (default) or baseline.
                    enum:
                    - restricted
                    - baseline
                    type: string
                  runtimeClassName:
                    description: RuntimeClassName optionally pins preview pods to
                      a RuntimeClass (e.g. gvisor).
                    type: string
                type: object
              ttlSeconds:
                description: TTLSeconds default lifetime when PreviewEnvironment omits
                  ttlSeconds.
                format: int64
                minimum: 0
                type: integer
            type: object
          status:
            description: PreviewTemplateStatus is reserved for observed template state.
            properties:
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
