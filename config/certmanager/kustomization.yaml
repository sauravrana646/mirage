resources:
- issuer.yaml
- certificate-webhook.yaml
# metrics-certs is optional; enable with METRICS-WITH-CERTS patches in config/default.
# - certificate-metrics.yaml

configurations:
- kustomizeconfig.yaml
